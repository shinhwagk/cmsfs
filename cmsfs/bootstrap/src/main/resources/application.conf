play.application.loader = org.wex.cmsfs.bootstrap.ServiceApplicationLoader

lagom {
  discovery {
    consul {
      agent-hostname = "consul.cmsfs.org"
      agent-port = 8500
      uri-scheme = "http"
      routing-policy = "round-robin"
    }
  }
}